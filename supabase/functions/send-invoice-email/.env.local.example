# ============================================================================
# Send Invoice Email Edge Function - Environment Variables
# ============================================================================
# 
# This file contains environment variables for the send-invoice-email Edge Function.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local (it's gitignored and won't be committed)
# 2. Fill in your actual values below
# 3. When serving the function locally, use: 
#    supabase functions serve send-invoice-email --env-file .env.local
#
# PRODUCTION:
# For production deployments, set these as environment variables on your
# Edge Functions container/deployment platform (e.g., Coolify, Docker, etc.)
# ============================================================================

# ----------------------------------------------------------------------------
# Resend API Configuration (Required)
# ----------------------------------------------------------------------------

# Resend API Key
# Get your API key from: https://resend.com/api-keys
# Format: re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_API_KEY=re_your_api_key_here

# From Email Address
# IMPORTANT: Must use an email address from a domain you've verified in Resend
# Example: invoice@vierweb.no (where vierweb.no is verified in Resend dashboard)
# This is the email address that will appear as the sender
RESEND_FROM_EMAIL=invoice@yourdomain.com

# From Name (Optional)
# The display name for the sender (e.g., "Invoice Manager", "Acme Corp")
# Defaults to "Invoice Manager" if not set
RESEND_FROM_NAME=Invoice Manager

# ----------------------------------------------------------------------------
# Fallback SMTP Configuration (Optional)
# ----------------------------------------------------------------------------
# These variables are used as fallbacks if Resend-specific variables aren't set.
# Useful if you're migrating from SMTP or want a backup configuration.
# Uncomment and fill in if needed.

# SMTP_FROM_EMAIL=invoice@yourdomain.com
# SMTP_ADMIN_EMAIL=invoice@yourdomain.com
# SMTP_SENDER_NAME=Invoice Manager

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# - The .env.local file is gitignored (via *.local pattern in .gitignore)
# - Never commit your actual API keys or credentials
# - For production, use your deployment platform's environment variable system
# - Verify your domain in Resend before using it: https://resend.com/domains
